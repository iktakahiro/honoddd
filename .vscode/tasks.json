{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "kill-port-3000",
      "type": "shell",
      "command": "pid=$(lsof -tiTCP:3000 -sTCP:LISTEN); if [ -n \"$pid\" ]; then kill $pid; fi",
      "problemMatcher": []
    },
    {
      "label": "kill-port-3001",
      "type": "shell",
      "command": "pid=$(lsof -tiTCP:3001 -sTCP:LISTEN); if [ -n \"$pid\" ]; then kill $pid; fi",
      "problemMatcher": []
    }
  ]
}
